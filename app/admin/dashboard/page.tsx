// Admin Dashboard: Overview and quick actions
import React from 'react';
const AdminDashboard = () => {
  // Fetch stats: articles by status, withdrawals, users, etc.
  return (
    <div>
      <h1>Admin Dashboard</h1>
      {/* Stats: Articles (approved/rejected/pending), Withdrawals, Users */}
      {/* Quick links to moderation, boosting, withdrawals, members, settings */}
    </div>
  );
};
export default AdminDashboard;
